{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug cargo run (print dice.png)",
      "cargo": {
        "args": [
          "run",
          "--bin",
          "pngme"
        ]
      },
      "args": ["print", "./dice.png"],
      "cwd": "${workspaceFolder}"
    }
  ]
}